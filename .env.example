# Réponse Éclair — variables d'environnement
#
# 1. Copie ce fichier :        cp .env.example .env.local
# 2. Remplis les deux valeurs depuis Supabase > Project Settings > API
#
# .env.local n'est JAMAIS committé (voir .gitignore).
# Ce fichier-ci, .env.example, est committé mais NE CONTIENT AUCUN secret.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
